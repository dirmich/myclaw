feat: MyClaw 애플리케이션 Docker 빌드 환경 구성

- 프로젝트 기반인 `Bun` 환경에 맞게 `oven/bun:alpine`을 베이스로 한 멀티 스테이지 `Dockerfile` 작성 (Next.js standalone 빌드 최적화 적용)
- Docker 빌드 최적화를 위한 `.dockerignore` 추가 
- `next.config.ts`의 Build Output을 `standalone`으로 변경하여 컨테이너 사이즈 최소화
- 손쉬운 배포를 위해 3000 포트를 매핑한 기본 `docker-compose.yml` 추가
